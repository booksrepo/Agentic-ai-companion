anthropic>=0.40.0
httpx>=0.27.0
mcp>=1.0.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
pytest>=8.0.0

# Notes:
# - Not every chapter needs every package; install what a given chapter's
#   README asks for. This file is a superset for convenience.
# - Chapter 2's Terraform demo requires the Terraform CLI, not a Python
#   package: https://developer.hashicorp.com/terraform/install
# - Check your provider's current SDK docs before pinning exact versions —
#   this book's own premise is that exact library syntax goes stale faster
#   than the underlying patterns do.
